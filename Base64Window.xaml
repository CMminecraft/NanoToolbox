<UserControl x:Class="Toolbox.Base64Window"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部横幅：图标 + 标题 + 副标题 -->
        <Border Grid.Row="0" Margin="0,0,0,14" Padding="14,12" CornerRadius="12"
                Background="{DynamicResource BackgroundCardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <Border Width="40" Height="40" CornerRadius="10" Background="{DynamicResource AccentPinkBrush}">
                    <Image x:Name="HeaderIcon" Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Border>
                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Base64 转换" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="文本与文件双向 Base64 编码 / 解码，支持 URL-Safe" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 工具栏 -->
        <Border Grid.Row="1" Margin="0,0,0,10" Padding="10,8" CornerRadius="10"
                Background="{DynamicResource BackgroundCardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <Button Content="编码 → Base64" Click="BtnEncode_Click" Style="{StaticResource BtnSecondary}" Margin="0,0,8,0"/>
                <Button Content="解码 ← Base64" Click="BtnDecode_Click" Style="{StaticResource BtnSecondary}" Margin="0,0,8,0"/>
                <Button Content="文件 → Base64" Click="BtnFileToBase64_Click" Style="{StaticResource BtnSecondary}" Margin="0,0,8,0"/>
                <Button Content="Base64 → 文件" Click="BtnBase64ToFile_Click" Style="{StaticResource BtnSecondary}" Margin="0,0,16,0"/>
                <CheckBox x:Name="UrlSafeCheck" Content="URL-Safe（- _ 代替 + /）" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,12,0"/>
                <CheckBox x:Name="IgnoreInvalidCheck" Content="解码时忽略非 Base64 字符" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
            </StackPanel>
        </Border>

        <!-- 输入区 -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="InputTitle" Text="输入" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="14,10,14,6"/>
                <TextBox Grid.Row="1" x:Name="InputText"
                         AcceptsReturn="True" AcceptsTab="True"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="13" Padding="14,4,14,14"
                         BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}"
                         TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <!-- 操作行 -->
        <Grid Grid.Row="3" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Text="就绪" FontSize="12" Foreground="{DynamicResource TextTertiaryBrush}" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="↑ 输入到输出" Click="BtnCopyInputToOutput_Click" Style="{StaticResource BtnSecondary}" Margin="0,0,8,0"/>
                <Button Content="复制结果" Click="BtnCopyResult_Click" Style="{StaticResource BtnSecondary}" Margin="0,0,8,0"/>
                <Button Content="清空" Click="BtnClear_Click" Style="{StaticResource BtnSecondary}"/>
            </StackPanel>
        </Grid>

        <!-- 输出区 -->
        <Border Grid.Row="4" Style="{StaticResource CardBorder}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="OutputTitle" Text="输出" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="14,10,14,6"/>
                <TextBox Grid.Row="1" x:Name="OutputText"
                         AcceptsReturn="True" AcceptsTab="True" IsReadOnly="False"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="13" Padding="14,4,14,14"
                         BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}"
                         TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <TextBlock Grid.Row="5" x:Name="InfoText" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}" Margin="2,8,0,0" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
